export enum PlayerState {
	Moving,
	Attacking,
	Dead,
	Respawning,
	None,
}

export enum PlayerGameState {
	Lobby,
	Voting,
	Playing,
}

export enum PlayerDirection {
	Up,
	Down,
	Left,
	Right,
	UpLeft,
	UpRight,
	DownLeft,
	DownRight,
	None,
}
